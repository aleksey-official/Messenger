package com.example.messenger.models;

import java.util.Date;

public class ChatMessage {
    public String senderId, receivedId, message, dataTime, senderFile, link, receiverFile, messageid, chatid, senderMessage;
    public Boolean read, delivered;
    public Date dataObject;
    public String conversionId, conversionName, conversionImage, conversionPhone, getConversionId;
}
